import { Msw } from "@/widgets/msw";

export default function MswPage() {
	return <Msw />;
}

export const metadata = {
	title: "Mock Server (MSW) | Documentation",
	description:
		"Learn how to use MSW for frontend development independent of backend."
};
